<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- App Theme (used after splash) -->
    <style name="Theme.DailyQuoteApp" parent="Theme.AppCompat.Light.NoActionBar">
<!--        <item name="colorPrimary">#6A1B9A</item>-->
<!--        <item name="colorPrimaryVariant">#4A148C</item>-->
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Splash Theme (used in SplashActivity only) -->
    <style name="Theme.DailyQuoteApp.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_splash_gradient</item>
    </style>

</resources>
